import * as THREE from "three";

class Station extends Model3D {
    #stationName;
    #metroArrvingTimes;

    // function to get arrving time of metro
    getMetroArrvingTime(Metro) {
        //TODO: heeft nog een body nodig
    }
}